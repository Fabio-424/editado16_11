import 'package:flutter/material.dart';

class GerenciarQuestionario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gerenciar questionários'),
      ),
      body: Center(child: Text('Conteúdo da página de questionários')),
    );
  }
}
